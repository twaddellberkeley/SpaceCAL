from rPINode_Display.rPI_Display import *
import pytest
# Check the projectors

# Check to see if they can be reached.
# Check to make sure they can be turned on
# Check to make sure video gets displayed
# Check to make sure projector gets turned off



"""
def motorConnected(adr):
    rc = motorConnectTest(adr)
    assert rc == 0, 'Could not connect to Motor %d' % adr


@pytest.mark.core
@pytest.mark.motor
def test_allMotors():
    for x in range(12, 17, 1):
        assert motorConnected(x)
"""